# Ansible - Home server automation 

automation: 
- common: basic setup 
- devtools: git (including config), vim, tmux, etc 
- docker: install docker 
- dotnet: install dotnet sdk

```bash
ansible-playbook playbook/site.yml
```
